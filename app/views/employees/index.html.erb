<h2>Employees</h2>
<%= form_for Employee.new do |f| %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>

<% @employees.each do |employee| %>
  <%= link_to employee.name, employee_path(employee) %> 
  <br>
<% end %>